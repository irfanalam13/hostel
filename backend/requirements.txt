django
djangorestframework
djangorestframework-simplejwt
# Production WSGI server + static file serving (Phase 8 infrastructure)
gunicorn
whitenoise
django-filter
python-dateutil
weasyprint
# PDF text extraction for the AI knowledge base (apps.aiknowledge ingestion)
pypdf>=6.14.2
psycopg[binary]>=3.3.4
Pillow>=10.0
celery
redis
pywebpush
drf-spectacular
drf-spectacular-sidecar
django-cors-headers
django-environ
django-axes
cryptography
# DNS lookups for custom-domain verification (TXT/CNAME) — apps.domains
dnspython
# Optional S3-compatible media storage (STORAGE_BACKEND=s3): AWS S3 / MinIO / R2
django-storages[boto3]
# Optional: error tracking. Activated only when SENTRY_DSN is set (see settings.py).
sentry-sdk
# Prometheus metrics at /metrics. Wired in only when PROMETHEUS_ENABLED=True.
django-prometheus
# Edge-security foundation (apps.security): YAML policy layer (SECURITY_CONFIG_FILE)
PyYAML
drf_spectacular_sidecar
